# EGotchi — Next Steps

- **Add animations** — use `Animated` or `react-native-reanimated` to bounce/wiggle the face when petted or when happiness changes
- **Hunger/energy stats** — add a second meter alongside happiness so there are multiple ways for the pet to decline
- **Notifications** — use `expo-notifications` to remind the user when the pet is getting sad (happiness ≤ 1) even while the app is in the background
- **Theming / name** — let the user name their pet and pick a background colour; store both in AsyncStorage alongside the face state
